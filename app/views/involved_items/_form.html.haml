= form_for @involved_item do |f|
  - if @involved_item.errors.any?
    #error_explanation
      %h2= "#{pluralize(@involved_item.errors.count, "error")} prohibited this involved_item from being saved:"
      %ul
        - @involved_item.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :product
    -#= f.text_field :product
  .field
    = f.label :customer
    -#= f.text_field :customer
  .field
    = f.label :quantity
    = f.number_field :quantity
  .field
    -#= f.label :transaction_type
    -#= f.text_field :transaction_type
    = f.hidden_field :trasaction_type, :value => ""
  .actions
    = f.submit 'Save'
